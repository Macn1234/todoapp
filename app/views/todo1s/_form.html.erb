<%= form_with(model: todo1, local: true) do |form| %>
  <% if todo1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo1.errors.count, "error") %> prohibited this todo1 from being saved:</h2>

      <ul>
      <% todo1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :todo1_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
